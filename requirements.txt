langchain>=0.3.0
langchain-groq>=0.2.0
langchain-google-genai>=2.0.0
langgraph>=0.2.0
pydantic>=2.7.0

fastapi
uvicorn
websockets
geopy
requests
httpx

# Auth — JWT tokens + password hashing
python-jose[cryptography]>=3.3.0
bcrypt>=4.0.0

# Layer 2 — LlamaIndex RAG pipeline
llama-index-core>=0.10.0
llama-index-llms-groq>=0.1.0

# Layer 3 — Ontology graph
networkx>=3.0
h3>=4.1.0
shapely>=2.0.0

# Layer 4 — Kafka-lite event stream (optional; falls back to asyncio.Queue)
redis>=5.0.0
